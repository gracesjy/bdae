```
CREATE TABLE STEP_MASTER (
  NAME VARCHAR2(40),
  FROM_NODE VARCHAR2 (40),
  TO_NODE VARCHAR2(40),
  START_FLAG VARCHAR2(10),
  END_FLAG VARCHAR2(10)
);

TRUNCATE TABLE STEP_MASTER;

INSERT INTO STEP_MASTER VALUES('X1', '1', '2', 'Y', NULL);
INSERT INTO STEP_MASTER VALUES('X2', '2', '3', NULL, NULL);
INSERT INTO STEP_MASTER VALUES('X3', '3', '4', NULL, NULL);
INSERT INTO STEP_MASTER VALUES('X4', '4', 'Close', NULL, 'Y');

INSERT INTO STEP_MASTER VALUES('X5', 'Close', NULL, NULL, 'Y');
COMMIT;

SELECT * FROM STEP_MASTER;

SELECT *
      FROM STEP_MASTER
START WITH FROM_NODE ='1' AND START_FLAG='Y'
CONNECT BY PRIOR TO_NODE = FROM_NODE;
```
