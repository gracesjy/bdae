###
```


SELECT A.STEP_ID, A.EQP_TYPE, A.RECIPE_ID, D.PARAM_TYPE, C.PARAM_VALS,
       D.PARAM_SRCH_VALS
FROM STEP_MASTER A, ACTION_MASTER B, ACTION_SLOT C, SLOT_PARAMETER_MAP D
WHERE 1=1
  AND A.FCT_ID = B.FCT_ID
  AND A.MAIN_PRODUCT_ID = B.MAIN_PRODUCT_ID
  AND A.PROCESS_ID = B.PROCESS_ID
  AND A.STEP_SEQ = B.STEP_SEQ
  AND A.FCT_ID = C.FCT_ID
  AND B.PROCESS_ID = C.PROC_ID
  AND B.STEP_SEQ = C.STEP_SEQ
  AND B.ACTION_SEQ = C.ACTION_SEQ
  AND C.SLOT_ID = D.SLOT_ID
  AND C.PARAM_ID = D.PARAM_ID
  AND A.FCT_ID = 'P4A'
  AND A.MAIN_PRODUCT_ID = 'E2B'
  AND A.PROCESS_ID = 'E2BC05'
  AND A.STEP_SEQ = 'E2BC05_002'
  AND B.ACTION_SEQ = 'B-05-019_001'
  
1) A, B 만 정리

SELECT A.STEP_ID, A.EQP_TYPE, A.RECIPE_ID
FROM STEP_MASTER A, ACTION_MASTER B
WHERE 1=1
  AND A.FCT_ID = B.FCT_ID
  AND A.MAIN_PRODUCT_ID = B.MAIN_PRODUCT_ID
  AND A.PROCESS_ID = B.PROCESS_ID
  AND A.STEP_SEQ = B.STEP_SEQ
  -- AND A.FCT_ID = C.FCT_ID
  -- AND B.PROCESS_ID = C.PROC_ID
  -- AND B.STEP_SEQ = C.STEP_SEQ
  -- AND B.ACTION_SEQ = C.ACTION_SEQ
  -- AND C.SLOT_ID = D.SLOT_ID
  -- AND C.PARAM_ID = D.PARAM_ID
  AND A.FCT_ID = 'P4A'
  AND A.MAIN_PRODUCT_ID = 'E2B'
  AND A.PROCESS_ID = 'E2BC05'
  AND A.STEP_SEQ = 'E2BC05_002'
  AND B.ACTION_SEQ = 'B-05-019_001';
  
2) B,C 
SELECT B.*, C.*
FROM ACTION_MASTER B, ACTION_SLOT C
WHERE 1=1
  -- AND A.FCT_ID = B.FCT_ID
  -- AND A.MAIN_PRODUCT_ID = B.MAIN_PRODUCT_ID
  -- AND A.PROCESS_ID = B.PROCESS_ID
  -- AND A.STEP_SEQ = B.STEP_SEQ
  -- AND A.FCT_ID = C.FCT_ID
  AND B.PROCESS_ID = C.PROC_ID
  AND B.STEP_SEQ = C.STEP_SEQ
  AND B.ACTION_SEQ = C.ACTION_SEQ
  -- AND C.SLOT_ID = D.SLOT_ID
  -- AND C.PARAM_ID = D.PARAM_ID
  -- AND A.FCT_ID = 'P4A'
  AND B.FCT_ID = C.FCT_ID
  AND B.FCT_ID = 'P4A'
  AND B.MAIN_PRODUCT_ID = 'E2B'
  AND B.STEP_SEQ = 'E2BC05_002'
  -- AND A.MAIN_PRODUCT_ID = 'E2B'
  -- AND A.PROCESS_ID = 'E2BC05'
  -- AND A.STEP_SEQ = 'E2BC05_002'
  AND B.ACTION_SEQ = 'B-05-019_001';
  
 3) C,D
SELECT C.*, D.*
FROM ACTION_SLOT C, SLOT_PARAMETER_MAP D
WHERE 1=1
  -- AND A.FCT_ID = B.FCT_ID
  -- AND A.MAIN_PRODUCT_ID = B.MAIN_PRODUCT_ID
  -- AND A.PROCESS_ID = B.PROCESS_ID
  -- AND A.STEP_SEQ = B.STEP_SEQ
  -- AND A.FCT_ID = C.FCT_ID
  -- AND B.PROCESS_ID = C.PROC_ID
  -- AND B.STEP_SEQ = C.STEP_SEQ
  -- AND B.ACTION_SEQ = C.ACTION_SEQ
  AND C.SLOT_ID = D.SLOT_ID
  AND C.PARAM_ID = D.PARAM_ID
  AND C.FCT_ID = 'P4A'
  -- AND A.MAIN_PRODUCT_ID = 'E2B'
  -- AND A.PROCESS_ID = 'E2BC05'
  AND C.PROC_ID = 'E2BC05'
  AND C.STEP_SEQ = 'E2BC05_002'
  AND C.ACTION_SEQ = 'B-05-019_001' 
```
